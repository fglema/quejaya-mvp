---
permalink: "/index.html"
---
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>QuejaYA.pe</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <style>
    body {
      font-family: sans-serif;
      text-align: center;
      max-width: 600px;
      margin: 4rem auto;
      padding: 0 1rem;
      background: #f7f7f7;
    }
    header { margin-bottom: 1rem; }
    .logo img { max-width: 200px; }
    h1 {
      color: #d92b2b;
      font-size: 2rem;
      margin-bottom: .5rem;
    }
    p.subtitle {
      color: #555;
      font-size: 1.1rem;
      margin-bottom: 2rem;
    }
    .roles {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    @media (min-width:480px) {
      .roles {
        flex-direction: row;
        justify-content: center;
      }
    }
    .roles button {
      flex: 1;
      padding: 1rem;
      border: 2px solid #d92b2b;
      border-radius: 6px;
      background: white;
      color: #d92b2b;
      font-weight: bold;
      cursor: pointer;
      transition: background .2s, color .2s;
    }
    .roles button:hover {
      background: #d92b2b;
      color: white;
    }
    .identity-btn {
      margin-top: 1.5rem;
      font-size: 0.9rem;
      background: none;
      border: none;
      color: #666;
      cursor: pointer;
    }
    .identity-btn:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <header>
    <a href="/" class="logo">
      <img src="/images/logo.png" alt="QuejaYA.pe">
    </a>
  </header>

  <h1>¡Te escuchamos!</h1>
  <p class="subtitle">Gestiona tus reclamos de forma fácil y rápida.</p>

  <div class="roles">
    <button id="btn-consumer">Soy consumidor</button>
    <button id="btn-company">Soy empresa</button>
  </div>
  <button class="identity-btn" id="btn-signup">¿No tienes cuenta? Regístrate</button>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      document.getElementById('btn-consumer').onclick = () => {
        location.href =
          '/.netlify/functions/auth/login?redirect=' +
          encodeURIComponent('/consumer/index.html');
      };
      document.getElementById('btn-company').onclick = () => {
        location.href =
          '/.netlify/functions/auth/login?redirect=' +
          encodeURIComponent('/dashboard.html');
      };
      document.getElementById('btn-signup').onclick = () => {
        location.href = '/.netlify/functions/auth/login?screen_hint=signup';
      };
    });
  </script>
</body>
</html>
